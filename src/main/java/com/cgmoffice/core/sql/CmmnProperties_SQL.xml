<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace='cmmnproperties'>

	<select id="getQrUrl"
		resultType="string"
	>
	/* cmmnproperties.getQrUrl */
		SELECT
			CD_VAL1
		FROM
			TB_DTL_CD
		WHERE 1=1
			AND GRP_CD = 'QR_URL'
			AND CD = 'Q01'
	</select>

</mapper>