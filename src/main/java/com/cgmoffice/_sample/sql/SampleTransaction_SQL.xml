<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace='sample.sampleTracsaction'>

	<select id="getInfo"
		resultType="com.cgmoffice._sample.dto.Test01Dto"
	>
		SELECT
			A.USER_NAME
			,A.USER_AGE
		FROM
			SAMPLE_TEST01 A
		WHERE 1=1
			AND A.USER_NAME = '홍길동'
	</select>

	<update id="reset">
		UPDATE SAMPLE_TEST01 SET
			USER_AGE = 20
		WHERE 1=1
			AND USER_NAME = '홍길동'
	</update>

	<update id="test01" parameterType="int">
		UPDATE SAMPLE_TEST01 SET
			USER_AGE = ${age}
		WHERE 1=1
			AND USER_NAME = '홍길동'
	</update>

</mapper>